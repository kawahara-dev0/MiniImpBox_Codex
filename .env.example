# Safe local placeholders only. Copy to .env for local use and replace secrets locally.

DATABASE_URL="file:../data/sqlite/app.db"
BACKUP_DIR="/data/backups"
BETTER_AUTH_URL="http://localhost:3000"
BETTER_AUTH_SECRET="replace-with-a-local-secret-that-is-not-committed"
NODE_ENV="development"
